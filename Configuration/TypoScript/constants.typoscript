# customcategory=plugin.tx_formconsent=Form consent
# customsubcategory=pids=Page IDs

plugin.tx_formconsent {
  view {
    # cat=plugin.tx_formconsent/file; type=string; label=Path to template root (FE)
    templateRootPath =
    # cat=plugin.tx_formconsent/file; type=string; label=Path to template partials (FE)
    partialRootPath =
    # cat=plugin.tx_formconsent/file; type=string; label=Path to template layouts (FE)
    layoutRootPath =
  }

  persistence {
    # cat=plugin.tx_formconsent/pids; type=int+; label=Default storage PID for consent data
    storagePid = 0
  }
}
